# Il ramo privato: tutto cio' che non e' pubblicato.
#
# Oggi contiene il solo src/. Il livello si giustifica per simmetria con
# interface/ — e' la coppia interfaccia/implementazione a essere il disegno,
# non il numero di cartelle — ed e' anche dove finirebbe un eventuale
# secondo port, accanto a src/ e non dentro.
add_subdirectory(src)
